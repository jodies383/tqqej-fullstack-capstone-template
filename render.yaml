services:
  - type: web
    name: dealership-review-api
    runtime: python
    rootDir: server
    buildCommand: pip install -r requirements.txt && python manage.py migrate && python manage.py seed_sample_data
    startCommand: gunicorn dealer_portal.wsgi:application
